version: 2

sources:
  - name: s3_bucket 
    database: DBT_DEMO_SNOWFLAKE
    schema: DBT
    tables:
      - name: RAW_EVENTS
        config:
          freshness:
            warn_after: {count: 1, period: hour}
            error_after: {count: 1, period: day}
    